provider "aws" {
  # Any necessary provider configuration such as region
  # or the role to be used by Terraform can go here.
  # Though for the purposes of this demo, we probably
  # won't need to do anything here.

  # region  = "us-east-1"
}
